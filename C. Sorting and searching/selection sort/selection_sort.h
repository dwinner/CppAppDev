#pragma once

#include <stdlib.h>

/**
 * \brief Sorts array via selection sort
 * \param array Array to be sorted
 * \param arrayLength Array length
 */
void selection_sort(int array[], size_t arrayLength);
